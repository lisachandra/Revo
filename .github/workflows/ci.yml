name: CI

on:
  push:
    branches:
    - main

  pull_request:
    branches:
    - main

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - uses: Roblox/setup-foreman@v1
        with:
          version: "^1.0.1"
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: baptiste0928/cargo-install@v2
        with:
          crate: wally-package-types
          version: "^1"

      - name: Download defintion files & packages
        shell: bash
        run: |
          wally install
          curl -O https://raw.githubusercontent.com/JohnnyMorganz/luau-lsp/main/scripts/globalTypes.d.lua
          curl -O https://gist.githubusercontent.com/zxibs/81fc195db5e0e374c63b98c262b99e1d/raw/
          curl -O https://gist.githubusercontent.com/zxibs/1b861e0123ae548f282b106818202afd/raw/
          curl -O https://gist.githubusercontent.com/zxibs/92756cffe2f8ee8fe801218cb2f88931/raw/

      - name: Analyze
        shell: bash
        run: |
          rojo sourcemap dev.project.json --output sourcemap.json
          wally-package-types --sourcemap sourcemap.json Packages/
          luau-lsp analyze --no-strict-dm-types --ignore=Packages/** --base-luaurc=.luaurc --sourcemap=sourcemap.json \
            --definitions=globalTypes.d.lua \
            --definitions=utils.d.lua \
            --definitions=roact.d.lua \
            --definitions=unc.d.lua \
            --definitions=loaded.d.lua \
            src/
        